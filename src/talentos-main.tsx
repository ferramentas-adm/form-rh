import React from "react";
import ReactDOM from "react-dom/client";
import TalentoForm from "./TalentoForm";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <TalentoForm />
  </React.StrictMode>,
);
